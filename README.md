# AI Searching Algorithms

This repository collects implementations of common search algorithms and related utilities.

Contents
- `algorithms/` — source code for algorithms (Python and/or JavaScript). See `algorithms/algorithms.py` and `algorithms/package.json`.

Quick start

1. Clone the repository:

```powershell
git clone https://github.com/eyoaladmasu2217/AI-Searching-Algorithms.git
cd "AI searching algorithms"
```

2. If the code in `algorithms/` is Node-based:

```powershell
cd algorithms
npm install
```

3. If the code is Python-based, run with your Python interpreter:

```powershell
python algorithms\algorithms.py
```

Contributing
- Open issues or submit pull requests.
